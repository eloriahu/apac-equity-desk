---
description: Give a finished APAC desk draft a senior editorial pass — sharpen the lead, cut repetition, keep the house format, facts, citations and uncertainty.
argument-hint: [optional: target length, e.g. 120 words]
---

Edit the current draft. Any constraint the user gave:

$ARGUMENTS

Read `${CLAUDE_PLUGIN_ROOT}/skills/desk-editor/SKILL.md` and follow it.
Do not collect new evidence; if evidence is missing, return a specific data
request instead. Preserve the draft's existing house format unless the user
asks to change it.
